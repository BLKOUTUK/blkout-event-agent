{
  "networkContributions": {
    "participantId": "string",
    "participantName": "string",
    "email": "string",
    "participantType": "string (Ally, QTIPOCOrganiser, Organisation, CommunityPartner)",
    "currentContributionPoints": "number",
    "lifetimeContributionPoints": "number",
    "partnershipLevel": "number",
    "collaborativeAchievements": [
      {
        "id": "string",
        "achievementId": "string",
        "achievementName": "string",
        "completedAt": "string (ISO date)"
      }
    ],
    "recentContributions": [
      {
        "id": "string",
        "contributionId": "string",
        "contributionName": "string",
        "timestamp": "string (ISO date)",
        "pointsAwarded": "number",
        "bqmImpact": "string (description of impact on BQM members)",
        "notes": "string"
      }
    ],
    "createdAt": "string (ISO date)",
    "updatedAt": "string (ISO date)"
  },
  "networkContributions": {
    "id": "string",
    "name": "string",
    "description": "string",
    "contributionValue": "number",
    "category": "string (EventHosting, ResourceProvision, Opportunity, Collaboration, Connection, Amplification)",
    "bqmImpactLevel": "number (1-5 scale of impact on BQM members)",
    "isEnabled": "boolean",
    "requiresVerification": "boolean",
    "createdAt": "string (ISO date)",
    "updatedAt": "string (ISO date)"
  },
  "collaborativeAchievements": {
    "id": "string",
    "name": "string",
    "description": "string",
    "category": "string (CrossCommunity, ResourceSharing, EventCollaboration, AdvocacyPartnership, MentorshipNetwork)",
    "recognitionBadgeUrl": "string",
    "contributionPointsRequired": "number",
    "participantsRequired": "number (minimum number of network participants involved)",
    "bqmImpactDescription": "string (description of how this achievement benefits BQM members)",
    "contributionsRequired": [
      {
        "contributionId": "string",
        "count": "number"
      }
    ],
    "isEnabled": "boolean",
    "createdAt": "string (ISO date)",
    "updatedAt": "string (ISO date)"
  },
  "pendingContributions": {
    "id": "string",
    "participantId": "string",
    "contributionId": "string",
    "bqmImpactDescription": "string (description of how this contribution benefits BQM members)",
    "evidenceUrls": ["string (URLs to evidence of contribution)"],
    "notes": "string",
    "metadata": "object",
    "status": "string (pending, verified, rejected)",
    "verifiedBy": "string",
    "verifiedAt": "string (ISO date)",
    "createdAt": "string (ISO date)"
  }
}
